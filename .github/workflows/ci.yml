name: CI

on:
  push:
    branches: [ main ]
    tags-ignore: [ '**' ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Checks plus unit tests (fullrun.py is not collected by default).
  py-checks:
    uses: chatmail/workflows/.github/workflows/py-checks.yml@main
